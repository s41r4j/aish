[package]
name = "aish"
version = "0.1.0"
edition = "2021"
description = "An AI-native shell that executes natural-language requests and explains their results."
license = "MIT"

[profile.release]
strip = true
lto = true
codegen-units = 1
